name: Release to npm

# Strict release pipeline for the noy-db-on storage adapters.
#
# Triggered on GitHub Release `published`. Runs the full verification
# gate (install + build + lint + typecheck + test) on a clean checkout
# at the release tag, and only publishes if every gate passes.
#
# Publishes with `--provenance` so the npm registry attests a
# verifiable cryptographic link between the tarball and this workflow
# run (supply-chain integrity). Requires `id-token: write` permission
# on the job.
#
# ── npm dist-tag routing ─────────────────────────────────────────────
#
#   • `latest` — the default install (`pnpm add @noy-db/on-password`). Used
#     for stable themed releases.
#   • `next`   — opt-in early-adopter channel (`pnpm add @noy-db/on-password@next`).
#
# Routing rules:
#   - `release` event with `prerelease: true` (the "Set as a pre-release"
#     checkbox on the GitHub Release form) → publishes with `--tag next`.
#   - `release` event with `prerelease: false` → publishes with `--tag latest`.
#   - Manual `workflow_dispatch` → uses the `tag` input (default `latest`).
#
# Both paths preserve `--provenance` and the strict verification gate.
# The chosen tag is echoed to the workflow log + the run summary before
# any publish runs, so an unintended target shows up immediately.
#
# Setup required (one-time, in repo Settings → Secrets and variables):
#   - NPM_TOKEN (npm automation token with publish access to @noy-db/*)
#   - GitHub Actions must have "Read and write permissions" under
#     Settings → Actions → General → Workflow permissions
#
# Manual dispatch is available for first-publish or re-publish debugging;
# it requires typing PUBLISH into the confirm field so accidental triggers
# can't publish by mistake. The `tag` input lets a maintainer override
# the dist-tag without touching this file.

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type PUBLISH to confirm manual trigger'
        required: true
        default: ''
      tag:
        description: 'npm dist-tag (latest | next | canary | rc | beta | alpha)'
        required: false
        default: 'latest'
  push:
    branches: [main]
    paths: ['.github/workflows/release.yml']

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  # ── Gate 1: strict verification ──────────────────────────────────────
  verify:
    name: Verify (strict)
    runs-on: ubuntu-latest
    if: github.event_name == 'release' || github.event.inputs.confirm == 'PUBLISH' || github.event_name == 'push'
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.release.tag_name || github.ref }}

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          package-manager-cache: false
          registry-url: 'https://registry.npmjs.org'

      - name: Enable corepack (pnpm)
        run: corepack enable

      - name: Get pnpm store path
        id: pnpm-store
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@v5
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-

      - name: Install (frozen lockfile)
        run: pnpm install --frozen-lockfile

      - name: Architecture invariants
        run: pnpm check:architecture

      - name: Build
        run: pnpm build

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      # ⚠️ THE PORTED VERSION OF THIS GATE COULD NEVER PASS. It read
      # `require('./package.json').version` from the ROOT manifest — the
      # lockstep canonical in noy-db-to, whose root carries a version. THIS
      # root is `private` and versionless, so the expression yielded the
      # STRING "undefined" and the comparison failed for every possible tag.
      # A premise true where it was written and false where it was pasted.
      # Measured 2026-09-01, before this workflow had ever run.
      #
      # Now asserted where the version logic already lives, against the
      # property that actually matters: EVERY package equals the tag, not
      # merely equals the others. It also proves the lockstep @noy-db/*
      # ranges are uniform and each dev pin is admitted by the peer range
      # beside it — the #1228 shape, invisible to the suite by construction.
      - name: Every package version matches the release tag
        if: github.event_name == 'release'
        run: |
          TAG="${{ github.event.release.tag_name }}"
          pnpm check:versions-uniform --expect "${TAG#v}"

      - name: One version across the repo (non-release runs)
        if: github.event_name != 'release'
        run: pnpm check:versions-uniform

      # git is not npm, made executable. This repo was extracted at
      # 0.7.0-pre.17; noy-db then cut 0.7.0 for all nine of these packages and
      # removed the family, so npm carried a version this repo had no record
      # of. Three sessions established that by hand. This asks the registry.
      #
      # ⚠️ It FAILS CLOSED — exit 2 means "could not determine", which is NOT
      # a pass. A registry timeout must never render as "the version is free".
      # This is a pre-flight that GATES a decision, so it must stay fatal:
      # `continue-on-error` looks like one knob and behaves like two, and is
      # backwards here.
      - name: Refuse a version npm already carries
        if: github.event_name == 'release'
        run: pnpm check:not-already-published

      # The dist-tag is routed entirely by the "Set as a pre-release" checkbox on
      # the Release form, so a forgotten tick publishes a pre-release to @latest.
      # Recovery is awkward but NOT impossible from CI, and the distinction
      # matters now that align-dist-tags.mjs in this repo depends on it:
      # `npm dist-tag add` DOES work with the automation token (verified by
      # klum-db against the live registry), because that token bypasses 2FA. It
      # is the LOCAL `~/.npmrc` path that needs an interactive OTP. `npm
      # deprecate` is a different endpoint and has NOT been tested this way —
      # do not widen this into "account-changing writes are automatable".
      #
      # ⚠️ THIS JUSTIFICATION HAS EXPIRED — kept as a record, not a reason.
      # The ported text said "@latest is already broken — all 17 on-*@0.5.0
      # versions are deprecated, and it cannot be repaired with anything short
      # of a stable release, which is blocked." Measured 2026-09-01: there are
      # NINE packages here, not 17 (17 is a to-* count); @latest is 0.7.0, a
      # STABLE, on all nine; and the packument's `deprecated` field is null.
      # The stable release it called blocked HAS SHIPPED.
      #
      # The GUARD stays, because it is right for the general reason rather
      # than the expired one: routing a semver pre-release to @latest makes it
      # the default install, and npm will not let you take that back. Generalise
      # the comment to the rule; do not extend the enumeration.
      #
      # It has also already happened here by a different route: to-browser-fs
      # debuted in 0.6.0-pre.2, and npm sets `latest` on a package's FIRST
      # publish regardless of --tag, so to-browser-fs@latest is pinned to a
      # pre-release and later `--tag next` publishes never move it. That path is
      # not what this step catches — no guard can, it is npm's own behaviour —
      # but it is the same end state, which is the point.
      #
      # Ported from noy-db-ui#22 via klum-db#81 / noy-db#1003; mechanism is
      # identical so all four repos behave the same.
      #
      # Only this direction is blocked: a STABLE version published to @next is
      # harmless and recoverable, so it stays allowed.
      - name: Refuse a semver pre-release routed to @latest
        if: github.event_name == 'release'
        run: |
          TAG="${{ github.event.release.tag_name }}"
          VERSION="${TAG#v}"
          if [ "${{ github.event.release.prerelease }}" != "true" ] && [ "${VERSION#*-}" != "$VERSION" ]; then
            echo "::error::$VERSION is a semver pre-release, but this Release is not marked as a pre-release."
            echo "::error::Publishing would route it to @latest, which is already broken here (17 deprecated 0.5.0 versions)."
            echo "::error::Tick 'Set as a pre-release' on the Release and re-run."
            exit 1
          fi
          echo "✅ routing check passed (version=$VERSION, prerelease=${{ github.event.release.prerelease }})"

  # ── Gate 1b: the declared peer range is TRUE at its floor ────────────
  #
  # `verify` above proves nothing about the peer range: every one of its gates
  # runs against the DEV PIN, so all of them stay green no matter how wrong the
  # range is. Only this job installs a different hub.
  #
  # It lives in its own workflow rather than as a step in `verify` on purpose —
  # `verify` carries a pnpm store cache, and peer-floor.yml deliberately has
  # none, with a comment explaining why. Inlining it would put that cache in
  # front of the guard and leave the reasoning stranded in a file that no
  # longer holds the check.
  #
  # `ref` must be passed: the called workflow would otherwise check out the
  # default branch and gate `main` instead of the tag being published.
  peer-floor:
    name: Peer floor
    if: github.event_name == 'release' || github.event.inputs.confirm == 'PUBLISH' || github.event_name == 'push'
    uses: ./.github/workflows/peer-floor.yml
    with:
      ref: ${{ github.event.release.tag_name || github.ref }}

  # ── Gate 2: publish with provenance ──────────────────────────────────
  #
  # Explicit event guard: publish only runs on:
  #   - a `release` event (someone created a GitHub Release), or
  #   - a `workflow_dispatch` with `confirm: PUBLISH` typed in.
  #
  # The `verify` job above also runs on `push` to main when this file
  # changes (so workflow edits get tested), but the publish step
  # MUST NOT run on that path — otherwise pushing a workflow edit
  # could auto-publish to npm without explicit confirmation.
  publish:
    name: Publish to npm
    needs: [verify, peer-floor]
    if: |
      github.event_name == 'release' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.confirm == 'PUBLISH')
    runs-on: ubuntu-latest
    outputs:
      dist_tag: ${{ steps.dist_tag.outputs.tag }}
    permissions:
      contents: read
      id-token: write   # required for npm --provenance
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.release.tag_name || github.ref }}

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          package-manager-cache: false
          registry-url: 'https://registry.npmjs.org'

      - name: Enable corepack (pnpm)
        run: corepack enable

      - name: Get pnpm store path
        id: pnpm-store
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@v5
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-

      - name: Install (frozen lockfile)
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      # Resolve the npm dist-tag for this run. Computed once + echoed
      # to the run summary so an unintended target shows up before any
      # publish runs. Allowlist guards against typos in workflow_dispatch
      # input — the publish step refuses to run with an unknown tag.
      - name: Resolve npm dist-tag
        id: dist_tag
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            if [ "${{ github.event.release.prerelease }}" = "true" ]; then
              TAG="next"
              REASON="release event, prerelease=true"
            else
              TAG="latest"
              REASON="release event, prerelease=false"
            fi
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ github.event.inputs.tag }}"
            REASON="workflow_dispatch tag input"
          else
            TAG="latest"
            REASON="default"
          fi

          case "$TAG" in
            latest|next|canary|rc|beta|alpha) ;;
            *)
              echo "❌ Refusing to publish: tag '$TAG' is not in the allowlist (latest|next|canary|rc|beta|alpha)."
              exit 1
              ;;
          esac

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "### npm dist-tag" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Tag:** \`$TAG\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Reason:** $REASON" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          if [ "$TAG" = "latest" ]; then
            echo "Consumers run \`pnpm add @noy-db/on-password\` (default install)." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "Consumers run \`pnpm add @noy-db/on-password@$TAG\` (opt-in channel)." >> "$GITHUB_STEP_SUMMARY"
          fi

      # Check which npm account the token belongs to. If this step fails,
      # the token is invalid; if it shows the wrong user, the user's account
      # doesn't own @noy-db/*.
      - name: Check npm auth (whoami)
        run: |
          WHOAMI=$(npm whoami 2>&1) && echo "npm user: $WHOAMI" || echo "npm whoami failed: $WHOAMI"
          echo "npm user: ${WHOAMI:-failed}" >> "$GITHUB_STEP_SUMMARY"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        continue-on-error: true

      # Recursive publish over all @noy-db/on-* stores. `--no-git-checks`
      # because the publish job checks out a tag, not a branch — pnpm's
      # default branch-clean check would fail otherwise. `--provenance`
      # requires id-token: write (granted on the job above). pnpm skips a
      # version already on the registry, so re-runs are safe.
      - name: Publish stores
        run: |
          set -o pipefail
          echo "Publishing @noy-db/on-* with --tag ${{ steps.dist_tag.outputs.tag }}"
          pnpm -r publish \
            --access public \
            --tag '${{ steps.dist_tag.outputs.tag }}' \
            --no-git-checks \
            --provenance 2>&1 | tee /tmp/publish.log
          EXIT=${PIPESTATUS[0]}
          if [ "$EXIT" -ne 0 ]; then
            echo "### Publish failed" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"; cat /tmp/publish.log >> "$GITHUB_STEP_SUMMARY"; echo '```' >> "$GITHUB_STEP_SUMMARY"
          fi
          exit "$EXIT"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Summarise
        run: |
          echo "### Published" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- Release: \`${{ github.event.release.tag_name || github.ref_name }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- npm dist-tag: \`${{ steps.dist_tag.outputs.tag }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- Provenance: enabled (look for the ✓ on https://www.npmjs.com/package/@noy-db/on-password)" >> "$GITHUB_STEP_SUMMARY"

  # ── Align dist-tags after a STABLE publish ───────────────────────────
  #
  # `release.yml` routes an unmarked Release to `--tag latest`, which sets
  # `latest` to the stable and LEAVES `next` on the last pre-release. Since
  # `0.6.0 > 0.6.0-pre.7`, `@next` then sits BEHIND `@latest` — the "lying tag"
  # state the family sweep flags, created by the act of cutting a stable.
  #
  # Pointing `next` at the stable too is self-correcting: the next pre-release
  # moves it forward and the normal invariant returns.
  #
  # NOT continue-on-error, deliberately, and unlike the docs bridge. That job is
  # non-fatal because a missing payload is recoverable and must not fail a
  # publish that already succeeded. This one is different: a package left behind
  # makes `npm i @noy-db/on-x@next` resolve something OLDER than `@latest` — a
  # wrong answer, not a missing nicety. It must be loud.
  #
  # Only fires when the publish actually routed to `latest`.
  align-dist-tags:
    name: Align dist-tags (stable only)
    needs: publish
    if: github.event_name == 'release' && needs.publish.outputs.dist_tag == 'latest'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.release.tag_name }}
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          package-manager-cache: false
          registry-url: 'https://registry.npmjs.org'
      - name: Show the plan
        run: node scripts/align-dist-tags.mjs --version="${GITHUB_REF_NAME#v}" --dry-run
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      - name: Align `next` onto the stable
        run: node scripts/align-dist-tags.mjs --version="${GITHUB_REF_NAME#v}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      - name: Report failure loudly
        if: failure()
        run: |
          {
            echo "### ⚠️ dist-tag alignment FAILED — the publish itself succeeded"
            echo ""
            echo "The script's own log distinguishes two cases and they need OPPOSITE actions."
            echo "Read it before touching anything:"
            echo ""
            echo "- **\`dist-tag add\` FAILED** — those packages genuinely need repair, and the"
            echo "  log lists the exact command per package."
            echo "- **refused on the before-state** — \`latest\` was not the expected version, so"
            echo "  nothing was written. Do NOT hand-repair; check that the stable actually"
            echo "  published and that the version passed in was right."
            echo ""
            echo "A package written-but-unconfirmed is NOT a failure and does not reach this"
            echo "block — \`npm view\` is CDN-served and lags writes. Repairing one of those"
            echo "hand-edits a correct release."
          } >> "$GITHUB_STEP_SUMMARY"

  # ── Notify noy-db-docs ───────────────────────────────────────────────
  #
  # Files a `doc-sync needed` issue in noy-db-docs naming every package and
  # version this release published.
  #
  # There is deliberately NO docs-bridge.json payload. A `docs-bridge` job was
  # inherited here from noy-db-to and could never have run: it invoked
  # scripts/docs-bridge/build-payload.mjs, which came with no extraction. And
  # porting it is not a copy — that script filters `d.name.startsWith('to-')`
  # BY DESIGN, to protect the producer parity noy-db-docs' sync/bridge.mjs
  # depends on. Run here it emits zero packages and trips its own non-empty
  # guard, so a verbatim port swaps a job that failed GREEN for one that fails
  # red and still never notifies anyone.
  #
  # What noy-db-docs actually consumes is the ISSUE BODY: its pin check reads
  # the package list from the doc-sync issue, never from the Release body
  # (measured — the sync issues carry the package lines; Release bodies carry
  # none). So the list below IS the delta, and this job is the whole bridge.
  #
  # `continue-on-error` at JOB level, deliberately: by the time this runs the
  # publish has already succeeded, and a docs-side outage must not turn a
  # completed publish into a red release. The cost is that "non-fatal" and
  # "invisible" are the same setting — which has silently eaten releases
  # elsewhere in this family — so the `if: failure()` step below writes a loud
  # summary line. Non-fatal, never silent.
  #
  # Requires DOCS_SYNC_TOKEN: a fine-grained PAT with Issues:write on
  # vLannaAi/noy-db-docs. This repo's own GITHUB_TOKEN cannot write there.
  # When it is absent the step fails LOUDLY rather than skipping, because a
  # bridge that quietly does nothing is the exact failure this job replaced.
  notify-docs:
    name: Notify noy-db-docs
    needs: publish
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.release.tag_name }}

      - name: Open or update the doc-sync issue
        env:
          GH_TOKEN: ${{ secrets.DOCS_SYNC_TOKEN }}
          TAG: ${{ github.event.release.tag_name }}
          CHANNEL: ${{ needs.publish.outputs.dist_tag }}
          RELEASE_URL: ${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ github.event.release.tag_name }}
        run: |
          set -euo pipefail

          if [ -z "${GH_TOKEN:-}" ]; then
            echo "::error::DOCS_SYNC_TOKEN is not set — noy-db-docs was NOT notified of this publish."
            echo "::error::Add a fine-grained PAT with Issues:write on vLannaAi/noy-db-docs, or file the doc-sync by hand."
            exit 1
          fi

          # The package list is the payload. Enumerated INSIDE node rather than
          # by a shell glob, which is load-bearing: with no matches bash leaves
          # the glob LITERAL, node throws MODULE_NOT_FOUND, and `set -e` kills
          # the step BEFORE the count is taken — so the guard below could never
          # fire in the one case it names, and the operator got a raw node stack
          # trace instead of a sentence. Private manifests are excluded too,
          # matching every other gate here: a repo whose only on-* directory was
          # private would otherwise file an issue naming an unpublished package.
          PACKAGES=$(node -e "
            const { readdirSync, existsSync, readFileSync } = require('node:fs')
            const out = readdirSync('.')
              .filter((d) => d.startsWith('on-') && existsSync(d + '/package.json'))
              .map((d) => JSON.parse(readFileSync(d + '/package.json', 'utf8')))
              .filter((j) => !j.private && j.name && j.version)
              .map((j) => j.name + '@' + j.version)
              .sort()
            process.stdout.write(out.join('\n'))
          ")
          COUNT=$(printf '%s\n' "$PACKAGES" | grep -c . || true)
          if [ "$COUNT" -eq 0 ]; then
            echo "::error::No on-* packages found — refusing to file an empty doc-sync issue."
            exit 1
          fi

          {
            echo "\`noy-db-on\` published **$TAG** to \`@$CHANNEL\`."
            echo ""
            echo "Published packages ($COUNT):"
            echo ""
            echo '```'
            echo "$PACKAGES"
            echo '```'
            echo ""
            echo "Run the doc-sync against these versions and update the pins."
            echo ""
            echo "There is no \`docs-bridge.json\` asset for this producer, by design — the payload"
            echo "builder is \`to-*\`-scoped, so **the list above is the delta**. Do not wait for an"
            echo "asset that will never arrive, and do not read its absence as nothing having shipped."
            echo ""
            echo "Release: $RELEASE_URL"
          } > /tmp/doc-sync.md

          cat /tmp/doc-sync.md >> "$GITHUB_STEP_SUMMARY"

          # TITLE is built ONCE and reused by the search, the exact-match filter
          # and the create. If those drift, the idempotency search stops matching
          # what `create` writes and every re-run files a duplicate, silently.
          # The repo name is not decoration: two producers can carry the same tag.
          #
          # The first match is taken IN JQ, not by `| head -1`: head closes the
          # pipe, the producer takes SIGPIPE, `pipefail` propagates it and
          # `set -e` kills the step — exit 141. That cannot happen with 0 or 1
          # matches (head reaches EOF first); it fires only with TWO OR MORE,
          # which is exactly the duplicate condition this search exists to
          # detect. The recovery path would have died in the one situation it
          # is for, and every normal release would have looked fine.
          TITLE="doc-sync needed: noy-db-on $TAG @$CHANNEL"

          NUM=$(gh issue list -R vLannaAi/noy-db-docs --state open --limit 20 \
            --search "$TITLE in:title" \
            --json number,title \
            --jq "[.[] | select(.title == \"$TITLE\") | .number] | first // empty")

          if [ -n "$NUM" ]; then
            gh issue comment "$NUM" -R vLannaAi/noy-db-docs --body-file /tmp/doc-sync.md
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "Commented on existing doc-sync issue #$NUM." >> "$GITHUB_STEP_SUMMARY"
          else
            gh issue create -R vLannaAi/noy-db-docs --title "$TITLE" --body-file /tmp/doc-sync.md
          fi

      # continue-on-error already swallowed any failure at job level, so this
      # cannot affect the publish. It exists so that "the docs were notified"
      # and "the run was green" stop being the same observation.
      - name: Surface a notify failure in the run summary
        if: failure()
        run: |
          {
            echo "## ⚠️ noy-db-docs was NOT notified — the publish succeeded, this did not"
            echo ""
            echo "\`${{ github.event.release.tag_name }}\` is published and installable."
            echo "Only the doc-sync notification failed, so **noy-db-docs does not know about it**."
            echo ""
            echo "File it by hand: an issue in \`vLannaAi/noy-db-docs\` titled"
            echo "\`doc-sync needed: noy-db-on ${{ github.event.release.tag_name }} @${{ needs.publish.outputs.dist_tag }}\`"
            echo "listing every published package and its version."
          } >> "$GITHUB_STEP_SUMMARY"
